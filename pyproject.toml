[project]
name = "ce_web"
version = "0.1.0"
description = "Repo for California Earrings website"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask==3.1.2",
    "gunicorn==23.0.0",
    "python-dotenv==1.2.1",
]
