repos:
  - repo: local
    hooks:
      - id: trade-show-social
        name: Generate changed trade-show previews with GPT Images
        entry: uv run --extra dev --extra imagegen python -m scripts.build_trade_show_social
        language: system
        pass_filenames: false
        always_run: true
